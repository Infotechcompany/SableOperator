# SableOperator

A lightweight Python starter project with an optimized text analytics core.

## Quick start

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
python -m unittest
```

## What is included

- Fast token normalization and frequency counting.
- Top-N term extraction with deterministic tie-breaking.
- Unit tests covering behavior and edge cases.
